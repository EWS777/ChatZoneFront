export interface FindPerson {
  connectionId: string
  themeList: number | null,
  country: number | null,
  city: number | null,
  age: number | null,
  gender: number | null,
  lang: number | null,
}
