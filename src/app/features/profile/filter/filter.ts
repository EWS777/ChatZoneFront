export interface Filter {
  themeList: number | null,
  country: number | null,
  city: number | null,
  age: number | null,
  gender: number | null,
  nativeLang: number | null,
  learnLang: number | null
}
