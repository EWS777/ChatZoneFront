export interface Filter {
  theme: number | null,
  country: number | null,
  city: number | null,
  age: number | null,
  yourGender: number | null,
  partnerGender: number | null,
  language: number | null
}
